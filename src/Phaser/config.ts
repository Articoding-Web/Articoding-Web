const config = {
    TILE_SIZE: 16,  // in pixels
    MOVEMENT_ANIMDURATION: 1000, // in milliseconds
    SPRITE_NAMES: [
        "player",
        "chest",
        "trap",
        "enemy",
        "background"
    ]
}

export default config;